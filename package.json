{
    "name": "wardstate",
    "version": "1.0.0",
    "scripts": {
      "start": "node server/server.js",
      "build": "npm install && npm run build:client && npm run install:server",
      "build:client": "cd server/client && npm install && npm run build",
      "install:server": "cd server && npm install"
    },
    "engines": {
      "node": ">=14.0.0"
    }
  }